# Install-guide screenshots

Annotated screenshots referenced by the install guides
([en](../en.md) · [de](../de.md) · [fr](../fr.md)) live here.

**Status:** not yet captured. The written steps in each guide are
complete on their own; these images are a visual aid to add later. The
guides reference them with italic placeholder captions, not broken image
links, so nothing renders broken while they're pending.

Screenshots to capture (macOS, English UI — the guides annotate per
language):

1. `01-drag-docker.png` — dragging Docker into the Applications folder.
2. `02-docker-running.png` — the Docker whale icon in the menu bar, running.
3. `03-terminal-pull.png` — Terminal showing a successful `docker pull`.
4. `04-config-textedit.png` — `claude_desktop_config.json` open in TextEdit with the `safe-fetch` entry.
5. `05-tool-listed.png` — the `fetch_url` tool listed in Claude Desktop's tools.

Keep images < 300 KB where possible; PNG preferred. Once added, replace
the italic placeholder captions in the guides with real `![alt](img/<file>)` references.
